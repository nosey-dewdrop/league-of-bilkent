{
  "permissions": {
    "allow": [
      "Bash(for f:*)",
      "Bash(javac -cp \".:lib/*\" model/*.java tools/*.java panels/*.java screens/*.java 2>&1)",
      "Bash(git rm:*)",
      "Bash(rm -rf build && mkdir -p build && javac -cp \".:lib/*\" -d build model/*.java tools/*.java panels/*.java screens/*.java 2>&1 | head -20)",
      "Bash(rm -rf build && mkdir -p build && javac -cp \".:lib/*\" -d build model/*.java tools/*.java panels/*.java screens/*.java 2>&1 | head -10)"
    ]
  }
}
